# Admin MVP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为项目补齐最小可用的后台管理系统，支持管理员登录、数据概览、用户列表、文章审核流和 AI 会话列表，并让前台只展示已审核通过的文章。

**Architecture:** 后端继续沿用 Spring Boot + Security + JPA 的现有结构，在 `users` 表增加 `role`、在 `articles` 表增加 `status`，通过新增 `/api/v1/admin/**` 接口提供后台能力。前端继续使用 Vue 3 + Pinia + Router，在现有用户登录态上增加 `role` 感知，并新增一套独立的 `/admin` 路由和布局，不与前台导航混用。

**Tech Stack:** Spring Boot 4, Spring Security, Spring Data JPA, H2/MySQL, Vue 3, Vite, Pinia, Vue Router, Element Plus, Node test runner

---

## 文件结构与职责

### 后端已有文件

- `backend/src/main/java/com/example/getoffer/entity/User.java`
  - 用户实体；本次新增 `role`
- `backend/src/main/java/com/example/getoffer/entity/Article.java`
  - 文章实体；本次新增 `status`
- `backend/src/main/java/com/example/getoffer/service/auth/AuthService.java`
  - 登录/注册/当前用户资料组装；本次补 `role`
- `backend/src/main/java/com/example/getoffer/dto/auth/UserProfileResponse.java`
  - 前端拿到的用户资料；本次补 `role`
- `backend/src/main/java/com/example/getoffer/service/article/ArticleService.java`
  - 文章创建、公开查询、我的文章；本次补审核状态和公开可见性
- `backend/src/main/java/com/example/getoffer/repository/ArticleRepository.java`
  - 文章查询；本次补状态查询和后台审核查询
- `backend/src/main/java/com/example/getoffer/controller/ArticleController.java`
  - 前台文章接口；保持接口路径不变
- `backend/src/main/java/com/example/getoffer/controller/UserController.java`
  - 个人中心接口；我的文章需要返回状态
- `backend/src/main/java/com/example/getoffer/config/SecurityConfig.java`
  - 安全配置；本次允许管理员接口继续走认证，权限在服务层再做强校验

### 后端新增文件

- `backend/src/main/java/com/example/getoffer/service/admin/AdminAccessService.java`
  - 统一封装“当前用户必须是管理员”的校验
- `backend/src/main/java/com/example/getoffer/service/admin/AdminDashboardService.java`
  - 汇总后台概览数据
- `backend/src/main/java/com/example/getoffer/service/admin/AdminUserService.java`
  - 管理端用户分页查询
- `backend/src/main/java/com/example/getoffer/service/admin/AdminArticleService.java`
  - 管理端文章审核、筛选、删除
- `backend/src/main/java/com/example/getoffer/service/admin/AdminAiSessionService.java`
  - 管理端 AI 会话分页查询
- `backend/src/main/java/com/example/getoffer/controller/AdminController.java`
  - 管理后台接口入口
- `backend/src/main/java/com/example/getoffer/dto/admin/AdminDashboardResponse.java`
  - 概览卡片返回
- `backend/src/main/java/com/example/getoffer/dto/admin/AdminUserResponse.java`
  - 用户管理表格行
- `backend/src/main/java/com/example/getoffer/dto/admin/AdminArticleResponse.java`
  - 文章审核表格行
- `backend/src/main/java/com/example/getoffer/dto/admin/AdminAiSessionResponse.java`
  - AI 会话表格行

### 前端已有文件

- `getoffer/src/router/index.ts`
  - 总路由入口；本次新增 `/admin` 路由
- `getoffer/src/router/route-access.ts`
  - 现有登录拦截工具；本次扩展管理员访问规则
- `getoffer/src/stores/admin.ts`
  - 用户登录态 store；本次补 `role` 和 `isAdmin`
- `getoffer/src/api/frontend.ts`
  - 现有 API 封装；本次新增后台 API
- `getoffer/src/api/transformers.ts`
  - 响应转换；本次补 `role` / `status`
- `getoffer/src/views/Profile.vue`
  - 个人中心；本次显示文章审核状态
- `getoffer/src/views/Upload.vue`
  - 发文页；提交成功后文案调整为“已提交审核”

### 前端新增文件

- `getoffer/src/views/admin/AdminLogin.vue`
  - 管理员登录页
- `getoffer/src/views/admin/AdminLayout.vue`
  - 后台整体布局
- `getoffer/src/views/admin/AdminDashboard.vue`
  - 数据概览页
- `getoffer/src/views/admin/AdminUsers.vue`
  - 用户管理页
- `getoffer/src/views/admin/AdminArticles.vue`
  - 文章审核页
- `getoffer/src/views/admin/AdminAiSessions.vue`
  - AI 会话页
- `getoffer/src/components/admin/AdminSidebar.vue`
  - 后台左侧菜单

### 测试文件

- 修改：`backend/src/test/java/com/example/getoffer/controller/AuthControllerTest.java`
- 修改：`backend/src/test/java/com/example/getoffer/controller/ArticleControllerTest.java`
- 修改：`backend/src/test/java/com/example/getoffer/controller/UserControllerTest.java`
- 新增：`backend/src/test/java/com/example/getoffer/controller/AdminControllerTest.java`
- 修改：`getoffer/tests/api-transformers.test.ts`
- 修改：`getoffer/tests/route-access.test.ts`
- 修改：`getoffer/tests/profile-view.test.ts`
- 新增：`getoffer/tests/admin-routes.test.ts`

---

### Task 1: 后端基础字段与认证返回

**Files:**
- Modify: `backend/src/main/java/com/example/getoffer/entity/User.java`
- Modify: `backend/src/main/java/com/example/getoffer/entity/Article.java`
- Modify: `backend/src/main/java/com/example/getoffer/dto/auth/UserProfileResponse.java`
- Modify: `backend/src/main/java/com/example/getoffer/dto/article/ArticleSummaryResponse.java`
- Modify: `backend/src/main/java/com/example/getoffer/service/auth/AuthService.java`
- Test: `backend/src/test/java/com/example/getoffer/controller/AuthControllerTest.java`
- Test: `backend/src/test/java/com/example/getoffer/controller/ArticleControllerTest.java`

- [ ] **Step 1: 先写失败测试，锁定 `role` 和文章默认 `status`**

```java
@Test
void registerReturnsRoleForNewUsers() throws Exception {
    mockMvc.perform(post("/api/v1/auth/register")
                    .contentType(MediaType.APPLICATION_JSON)
                    .content("""
                            {
                              "username": "admin_seed",
                              "phone": "13800138121",
                              "password": "123456"
                            }
                            """))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.data.user.role").value("USER"));
}

@Test
void createArticleStartsInPendingStatus() throws Exception {
    String token = TestAuthHelper.registerAndGetToken(mockMvc, "writer_pending", "13800138122", "123456");

    mockMvc.perform(post("/api/v1/articles")
                    .header("Authorization", "Bearer " + token)
                    .contentType(MediaType.APPLICATION_JSON)
                    .content("""
                            {
                              "title": "待审核文章",
                              "category": "前端",
                              "type": 1,
                              "tags": ["Vue"],
                              "content": "# draft"
                            }
                            """))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.data.status").value("PENDING"));
}
```

- [ ] **Step 2: 运行测试，确认现在确实失败**

Run: `./mvnw.cmd -Dtest=AuthControllerTest,ArticleControllerTest test`  
Expected: FAIL，提示 `role` 或 `status` 路径不存在。

- [ ] **Step 3: 最小实现字段、默认值和返回 DTO**

```java
// User.java
@Column(nullable = false, length = 20)
private String role = "USER";

public String getRole() { return role; }
public void setRole(String role) { this.role = role; }

// Article.java
@Column(nullable = false, length = 20)
private String status = "PENDING";

public String getStatus() { return status; }
public void setStatus(String status) { this.status = status; }

// UserProfileResponse.java
private String role;

public String getRole() { return role; }
public void setRole(String role) { this.role = role; }

// ArticleSummaryResponse.java
private String status;

public String getStatus() { return status; }
public void setStatus(String status) { this.status = status; }

// AuthService#toUserProfile
response.setRole(user.getRole());
```

- [ ] **Step 4: 在文章映射里把状态塞回响应**

```java
// ArticleService#toSummary
response.setStatus(article.getStatus());
```

- [ ] **Step 5: 重新运行同一组测试**

Run: `./mvnw.cmd -Dtest=AuthControllerTest,ArticleControllerTest test`  
Expected: PASS

- [ ] **Step 6: 提交这一小步**

```bash
git add backend/src/main/java/com/example/getoffer/entity/User.java backend/src/main/java/com/example/getoffer/entity/Article.java backend/src/main/java/com/example/getoffer/dto/auth/UserProfileResponse.java backend/src/main/java/com/example/getoffer/dto/article/ArticleSummaryResponse.java backend/src/main/java/com/example/getoffer/service/auth/AuthService.java backend/src/test/java/com/example/getoffer/controller/AuthControllerTest.java backend/src/test/java/com/example/getoffer/controller/ArticleControllerTest.java
git commit -m "feat: add role and article status fields"
```

### Task 2: 前台文章只展示已通过，个人中心显示全部状态

**Files:**
- Modify: `backend/src/main/java/com/example/getoffer/service/article/ArticleService.java`
- Modify: `backend/src/main/java/com/example/getoffer/repository/ArticleRepository.java`
- Modify: `backend/src/main/java/com/example/getoffer/dto/article/ArticleDetailResponse.java`
- Modify: `backend/src/test/java/com/example/getoffer/controller/ArticleControllerTest.java`
- Modify: `backend/src/test/java/com/example/getoffer/controller/UserControllerTest.java`

- [ ] **Step 1: 写失败测试，锁定公开接口过滤和我的文章保留状态**

```java
@Test
void publicArticleEndpointsOnlyExposeApprovedArticles() throws Exception {
    String token = TestAuthHelper.registerAndGetToken(mockMvc, "writer_public", "13800138123", "123456");

    String createResponse = mockMvc.perform(post("/api/v1/articles")
                    .header("Authorization", "Bearer " + token)
                    .contentType(MediaType.APPLICATION_JSON)
                    .content("""
                            {
                              "title": "不会公开",
                              "category": "后端",
                              "type": 1,
                              "tags": ["Spring"],
                              "content": "# hidden"
                            }
                            """))
            .andReturn()
            .getResponse()
            .getContentAsString();

    String articleId = JsonTestUtils.readJsonPath(createResponse, "$.data.id").toString();

    mockMvc.perform(get("/api/v1/articles").param("page", "1").param("pageSize", "10"))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.data.list.length()").value(0));

    mockMvc.perform(get("/api/v1/articles/" + articleId))
            .andExpect(status().isBadRequest());
}

@Test
void myArticlesIncludesStatusForPendingAndRejectedArticles() throws Exception {
    String token = TestAuthHelper.registerAndGetToken(mockMvc, "writer_status", "13800138124", "123456");

    mockMvc.perform(post("/api/v1/articles")
                    .header("Authorization", "Bearer " + token)
                    .contentType(MediaType.APPLICATION_JSON)
                    .content("""
                            {
                              "title": "个人中心状态",
                              "category": "前端",
                              "type": 1,
                              "tags": ["Vue"],
                              "content": "# test"
                            }
                            """))
            .andExpect(status().isOk());

    mockMvc.perform(get("/api/v1/users/me/articles")
                    .header("Authorization", "Bearer " + token)
                    .param("page", "1")
                    .param("pageSize", "10"))
            .andExpect(status().isOk())
            .andExpect(jsonPath("$.data.list[0].status").value("PENDING"));
}
```

- [ ] **Step 2: 运行失败测试**

Run: `./mvnw.cmd -Dtest=ArticleControllerTest,UserControllerTest test`  
Expected: FAIL，公开列表仍然能看到 `PENDING` 文章，或详情行为不符合预期。

- [ ] **Step 3: 在服务层为公开查询统一加 `APPROVED` 过滤**

```java
private Specification<Article> buildPublicSpecification(Integer type,
                                                        String category,
                                                        String tag,
                                                        String keyword,
                                                        Long authorId) {
    return (root, query, cb) -> {
        query.distinct(true);
        var predicate = cb.equal(root.get("status"), "APPROVED");
        // 继续拼 type/category/tag/keyword/authorId
        return predicate;
    };
}

public PageResult<ArticleSummaryResponse> getArticleList(...) {
    Specification<Article> specification = buildPublicSpecification(type, category, tag, keyword, authorId);
    ...
}

public List<ArticleSummaryResponse> getHotArticles(Integer type, int limit) {
    return articleRepository.findTop5ByStatusOrderByFavoriteCountDescViewCountDescCreatedAtDesc("APPROVED")
            .stream()
            .filter(article -> type == null || article.getType() == type)
            .limit(limit)
            .map(article -> toSummary(article, currentUserService.getCurrentUserOrNull()))
            .toList();
}
```

- [ ] **Step 4: 调整详情查询和仓库方法**

```java
// ArticleRepository.java
List<Article> findTop5ByStatusOrderByFavoriteCountDescViewCountDescCreatedAtDesc(String status);

// ArticleService#getDetail
Article article = articleRepository.findById(articleId)
        .orElseThrow(() -> new IllegalArgumentException("文章不存在"));
if (!"APPROVED".equals(article.getStatus())) {
    User currentUser = currentUserService.getCurrentUserOrNull();
    boolean owner = currentUser != null && currentUser.getId().equals(article.getAuthor().getId());
    if (!owner) {
        throw new IllegalArgumentException("文章不存在");
    }
}
```

- [ ] **Step 5: 重新跑同一组测试**

Run: `./mvnw.cmd -Dtest=ArticleControllerTest,UserControllerTest test`  
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add backend/src/main/java/com/example/getoffer/service/article/ArticleService.java backend/src/main/java/com/example/getoffer/repository/ArticleRepository.java backend/src/main/java/com/example/getoffer/dto/article/ArticleDetailResponse.java backend/src/test/java/com/example/getoffer/controller/ArticleControllerTest.java backend/src/test/java/com/example/getoffer/controller/UserControllerTest.java
git commit -m "feat: hide pending articles from public endpoints"
```

### Task 3: 后端管理员权限与概览/用户/会话接口

**Files:**
- Create: `backend/src/main/java/com/example/getoffer/service/admin/AdminAccessService.java`
- Create: `backend/src/main/java/com/example/getoffer/service/admin/AdminDashboardService.java`
- Create: `backend/src/main/java/com/example/getoffer/service/admin/AdminUserService.java`
- Create: `backend/src/main/java/com/example/getoffer/service/admin/AdminAiSessionService.java`
- Create: `backend/src/main/java/com/example/getoffer/dto/admin/AdminDashboardResponse.java`
- Create: `backend/src/main/java/com/example/getoffer/dto/admin/AdminUserResponse.java`
- Create: `backend/src/main/java/com/example/getoffer/dto/admin/AdminAiSessionResponse.java`
- Create: `backend/src/main/java/com/example/getoffer/controller/AdminController.java`
- Modify: `backend/src/main/java/com/example/getoffer/repository/UserRepository.java`
- Modify: `backend/src/main/java/com/example/getoffer/repository/AiSessionRepository.java`
- Test: `backend/src/test/java/com/example/getoffer/controller/AdminControllerTest.java`

- [ ] **Step 1: 写失败测试，锁定管理员权限和三类后台只读接口**

```java
@Test
void adminEndpointsRejectNormalUsers() throws Exception {
    String token = TestAuthHelper.registerAndGetToken(mockMvc, "normal_user", "13800138125", "123456");

    mockMvc.perform(get("/api/v1/admin/dashboard")
                    .header("Authorization", "Bearer " + token))
            .andExpect(status().isBadRequest());
}

@Test
void adminCanLoadDashboardUsersAndAiSessions() throws Exception {
    // 先注册管理员账号，再在测试里把 role 改成 ADMIN
    // 然后断言 dashboard/users/ai-sessions 都返回 200 和 data
}
```

- [ ] **Step 2: 运行测试，确认后台接口还不存在或权限不对**

Run: `./mvnw.cmd -Dtest=AdminControllerTest test`  
Expected: FAIL，提示 404 或接口返回不符合预期。

- [ ] **Step 3: 创建管理员权限守卫和 DTO**

```java
// AdminAccessService.java
@Service
public class AdminAccessService {
    private final CurrentUserService currentUserService;

    public AdminAccessService(CurrentUserService currentUserService) {
        this.currentUserService = currentUserService;
    }

    public User requireAdmin() {
        User user = currentUserService.requireCurrentUser();
        if (!"ADMIN".equals(user.getRole())) {
            throw new IllegalArgumentException("无权限访问后台");
        }
        return user;
    }
}
```

```java
// AdminDashboardResponse.java
public class AdminDashboardResponse {
    private long userCount;
    private long articleCount;
    private long aiSessionCount;
    // getter / setter
}
```

- [ ] **Step 4: 最小实现后台控制器和服务**

```java
@RestController
@RequestMapping("/api/v1/admin")
public class AdminController {

    @GetMapping("/dashboard")
    public ApiResponse<AdminDashboardResponse> dashboard() {
        return ApiResponse.success(adminDashboardService.getDashboard());
    }

    @GetMapping("/users")
    public ApiResponse<PageResult<AdminUserResponse>> users(...) {
        return ApiResponse.success(adminUserService.getUsers(...));
    }

    @GetMapping("/ai/sessions")
    public ApiResponse<PageResult<AdminAiSessionResponse>> aiSessions(...) {
        return ApiResponse.success(adminAiSessionService.getSessions(...));
    }
}
```

- [ ] **Step 5: 重新运行后台控制器测试**

Run: `./mvnw.cmd -Dtest=AdminControllerTest test`  
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add backend/src/main/java/com/example/getoffer/service/admin backend/src/main/java/com/example/getoffer/dto/admin backend/src/main/java/com/example/getoffer/controller/AdminController.java backend/src/main/java/com/example/getoffer/repository/UserRepository.java backend/src/main/java/com/example/getoffer/repository/AiSessionRepository.java backend/src/test/java/com/example/getoffer/controller/AdminControllerTest.java
git commit -m "feat: add admin dashboard and list endpoints"
```

### Task 4: 后端文章审核接口

**Files:**
- Create: `backend/src/main/java/com/example/getoffer/service/admin/AdminArticleService.java`
- Create: `backend/src/main/java/com/example/getoffer/dto/admin/AdminArticleResponse.java`
- Modify: `backend/src/main/java/com/example/getoffer/controller/AdminController.java`
- Modify: `backend/src/main/java/com/example/getoffer/repository/ArticleRepository.java`
- Modify: `backend/src/test/java/com/example/getoffer/controller/AdminControllerTest.java`

- [ ] **Step 1: 写失败测试，锁定文章审核、筛选和删除**

```java
@Test
void adminCanApproveRejectAndDeleteArticles() throws Exception {
    // 创建普通用户文章，默认 PENDING
    // 管理员调用 approve -> status=APPROVED
    // 管理员调用 reject -> status=REJECTED
    // 管理员调用 delete -> success=true
}
```

- [ ] **Step 2: 运行测试**

Run: `./mvnw.cmd -Dtest=AdminControllerTest test`  
Expected: FAIL，缺少 `/approve`、`/reject` 或后台文章列表字段。

- [ ] **Step 3: 增加后台文章服务和接口**

```java
@GetMapping("/articles")
public ApiResponse<PageResult<AdminArticleResponse>> articles(...) {
    return ApiResponse.success(adminArticleService.getArticles(page, pageSize, keyword, category, status));
}

@PatchMapping("/articles/{articleId}/approve")
public ApiResponse<AdminArticleResponse> approve(@PathVariable Long articleId) {
    return ApiResponse.success(adminArticleService.approve(articleId));
}

@PatchMapping("/articles/{articleId}/reject")
public ApiResponse<AdminArticleResponse> reject(@PathVariable Long articleId) {
    return ApiResponse.success(adminArticleService.reject(articleId));
}

@DeleteMapping("/articles/{articleId}")
public ApiResponse<Map<String, Object>> deleteArticle(@PathVariable Long articleId) {
    return ApiResponse.success(adminArticleService.delete(articleId));
}
```

- [ ] **Step 4: 在服务层统一处理状态流转**

```java
public AdminArticleResponse approve(Long articleId) {
    adminAccessService.requireAdmin();
    Article article = requireArticle(articleId);
    article.setStatus("APPROVED");
    return toResponse(articleRepository.save(article));
}

public AdminArticleResponse reject(Long articleId) {
    adminAccessService.requireAdmin();
    Article article = requireArticle(articleId);
    article.setStatus("REJECTED");
    return toResponse(articleRepository.save(article));
}
```

- [ ] **Step 5: 重新运行后台控制器测试**

Run: `./mvnw.cmd -Dtest=AdminControllerTest test`  
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add backend/src/main/java/com/example/getoffer/service/admin/AdminArticleService.java backend/src/main/java/com/example/getoffer/dto/admin/AdminArticleResponse.java backend/src/main/java/com/example/getoffer/controller/AdminController.java backend/src/main/java/com/example/getoffer/repository/ArticleRepository.java backend/src/test/java/com/example/getoffer/controller/AdminControllerTest.java
git commit -m "feat: add admin article moderation endpoints"
```

### Task 5: 前端登录态、转换器和管理员路由基础

**Files:**
- Modify: `getoffer/src/stores/admin.ts`
- Modify: `getoffer/src/api/transformers.ts`
- Modify: `getoffer/src/api/frontend.ts`
- Modify: `getoffer/src/router/route-access.ts`
- Modify: `getoffer/src/router/index.ts`
- Create: `getoffer/src/views/admin/AdminLogin.vue`
- Create: `getoffer/src/views/admin/AdminLayout.vue`
- Create: `getoffer/src/components/admin/AdminSidebar.vue`
- Test: `getoffer/tests/api-transformers.test.ts`
- Test: `getoffer/tests/route-access.test.ts`
- Test: `getoffer/tests/admin-routes.test.ts`

- [ ] **Step 1: 写失败测试，锁定 `role` 映射和管理员路由拦截**

```ts
test('mapUserProfile preserves role for admin routing', () => {
  const profile = mapUserProfile({ username: 'root', role: 'ADMIN', registerTime: '2026-04-23T08:00:00' })
  assert.equal(profile.role, 'ADMIN')
})

test('resolveRouteAccess redirects non-admin users away from admin routes', () => {
  assert.deepEqual(
    resolveRouteAccess({ fullPath: '/admin/dashboard', meta: { requiresAuth: true, requiresAdmin: true } }, true, false),
    { path: '/', query: { login: '1', redirect: '/admin/dashboard' } },
  )
})
```

- [ ] **Step 2: 跑前端单测，确认先失败**

Run: `node --test tests/api-transformers.test.ts tests/route-access.test.ts`  
Expected: FAIL，提示 `role` 或 `requiresAdmin` 逻辑不存在。

- [ ] **Step 3: 补 store、转换器和路由访问规则**

```ts
// stores/admin.ts
getters: {
  isLoggedIn: (state) => !!state.token,
  isAdmin: (state) => state.user?.role === 'ADMIN',
}

// transformers.ts
export const mapUserProfile = (profile: AnyRecord = {}): AnyRecord => ({
  ...profile,
  role: profile.role || 'USER',
  ...
})

// route-access.ts
meta?: {
  requiresAuth?: boolean
  requiresAdmin?: boolean
}

export const resolveRouteAccess = (
  to: RouteAccessInput,
  isLoggedIn: boolean,
  isAdmin = false,
): RouteAccessRedirect | null => {
  if (!isLoggedIn && to?.meta?.requiresAuth) {
    return { path: '/', query: { login: '1', redirect: to?.fullPath || '/' } }
  }
  if (to?.meta?.requiresAdmin && !isAdmin) {
    return { path: '/', query: { login: '1', redirect: to?.fullPath || '/' } }
  }
  return null
}
```

- [ ] **Step 4: 增加后台路由骨架**

```ts
const AdminLogin = () => import('@/views/admin/AdminLogin.vue')
const AdminLayout = () => import('@/views/admin/AdminLayout.vue')
const AdminDashboard = () => import('@/views/admin/AdminDashboard.vue')

{
  path: '/admin/login',
  name: 'adminLogin',
  component: AdminLogin,
  meta: { title: '后台登录' },
},
{
  path: '/admin',
  component: AdminLayout,
  meta: { requiresAuth: true, requiresAdmin: true },
  children: [
    { path: '', redirect: '/admin/dashboard' },
    { path: 'dashboard', name: 'adminDashboard', component: AdminDashboard, meta: { requiresAuth: true, requiresAdmin: true } },
  ],
}
```

- [ ] **Step 5: 重跑前端单测**

Run: `node --test tests/api-transformers.test.ts tests/route-access.test.ts tests/admin-routes.test.ts`  
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add getoffer/src/stores/admin.ts getoffer/src/api/transformers.ts getoffer/src/api/frontend.ts getoffer/src/router/route-access.ts getoffer/src/router/index.ts getoffer/src/views/admin/AdminLogin.vue getoffer/src/views/admin/AdminLayout.vue getoffer/src/components/admin/AdminSidebar.vue getoffer/tests/api-transformers.test.ts getoffer/tests/route-access.test.ts getoffer/tests/admin-routes.test.ts
git commit -m "feat: add admin auth and routing foundation"
```

### Task 6: 前端后台页面与文章审核交互

**Files:**
- Create: `getoffer/src/views/admin/AdminDashboard.vue`
- Create: `getoffer/src/views/admin/AdminUsers.vue`
- Create: `getoffer/src/views/admin/AdminArticles.vue`
- Create: `getoffer/src/views/admin/AdminAiSessions.vue`
- Modify: `getoffer/src/api/frontend.ts`
- Modify: `getoffer/src/views/Upload.vue`
- Test: `getoffer/tests/profile-view.test.ts`

- [ ] **Step 1: 写失败测试，锁定后台页面最小交互和发文提示文案**

```ts
test('upload view tells the user articles require admin approval after submission', () => {
  const source = readFileSync(resolve(process.cwd(), 'src/views/Upload.vue'), 'utf8')
  assert.match(source, /已提交审核/)
})

test('profile view contains article status labels', () => {
  const source = readFileSync(resolve(process.cwd(), 'src/views/Profile.vue'), 'utf8')
  assert.match(source, /PENDING|APPROVED|REJECTED/)
})
```

- [ ] **Step 2: 跑这组测试**

Run: `node --test tests/profile-view.test.ts`  
Expected: FAIL，说明前端还没展示审核语义。

- [ ] **Step 3: 新增后台 API 封装**

```ts
export const adminApi = {
  getDashboard() {
    return request({ url: '/api/v1/admin/dashboard', method: 'get' })
  },
  getUsers(params: AnyRecord = {}) {
    return request({ url: '/api/v1/admin/users', method: 'get', params })
  },
  getArticles(params: AnyRecord = {}) {
    return request({ url: '/api/v1/admin/articles', method: 'get', params })
  },
  approveArticle(articleId: number | string) {
    return request({ url: `/api/v1/admin/articles/${articleId}/approve`, method: 'patch' })
  },
  rejectArticle(articleId: number | string) {
    return request({ url: `/api/v1/admin/articles/${articleId}/reject`, method: 'patch' })
  },
  getAiSessions(params: AnyRecord = {}) {
    return request({ url: '/api/v1/admin/ai/sessions', method: 'get', params })
  },
}
```

- [ ] **Step 4: 落最小页面组件**

```vue
<!-- AdminArticles.vue -->
<template>
  <div class="admin-page">
    <el-segmented v-model="status" :options="statusOptions" />
    <el-table :data="rows">
      <el-table-column prop="title" label="标题" />
      <el-table-column prop="authorName" label="作者" />
      <el-table-column prop="status" label="状态" />
      <el-table-column label="操作">
        <template #default="{ row }">
          <el-button v-if="row.status === 'PENDING'" type="success" @click="approve(row.id)">通过</el-button>
          <el-button v-if="row.status === 'PENDING'" type="danger" @click="reject(row.id)">拒绝</el-button>
        </template>
      </el-table-column>
    </el-table>
  </div>
</template>
```

- [ ] **Step 5: 重跑视图相关测试**

Run: `node --test tests/profile-view.test.ts`  
Expected: PASS

- [ ] **Step 6: 提交**

```bash
git add getoffer/src/views/admin/AdminDashboard.vue getoffer/src/views/admin/AdminUsers.vue getoffer/src/views/admin/AdminArticles.vue getoffer/src/views/admin/AdminAiSessions.vue getoffer/src/api/frontend.ts getoffer/src/views/Upload.vue getoffer/tests/profile-view.test.ts
git commit -m "feat: add admin pages and moderation actions"
```

### Task 7: 个人中心文章状态展示

**Files:**
- Modify: `getoffer/src/views/Profile.vue`
- Modify: `getoffer/src/api/transformers.ts`
- Modify: `getoffer/tests/api-transformers.test.ts`
- Modify: `getoffer/tests/profile-view.test.ts`

- [ ] **Step 1: 写失败测试，锁定文章状态映射和个人中心状态标签**

```ts
test('mapArticle preserves moderation status', () => {
  const mapped = mapArticle({ id: 1, title: 'x', status: 'REJECTED', author: { username: 'alice' } })
  assert.equal(mapped.status, 'REJECTED')
})
```

- [ ] **Step 2: 跑失败测试**

Run: `node --test tests/api-transformers.test.ts tests/profile-view.test.ts`  
Expected: FAIL，`status` 未映射或页面未使用。

- [ ] **Step 3: 实现最小状态映射与 UI 标签**

```ts
// transformers.ts
export const mapArticle = (article: AnyRecord = {}): AnyRecord => ({
  ...,
  status: article.status || 'PENDING',
})
```

```vue
<!-- Profile.vue -->
<el-tag
  :type="article.status === 'APPROVED' ? 'success' : article.status === 'REJECTED' ? 'danger' : 'warning'"
>
  {{ article.status === 'APPROVED' ? '已通过' : article.status === 'REJECTED' ? '已拒绝' : '待审核' }}
</el-tag>
```

- [ ] **Step 4: 重跑测试**

Run: `node --test tests/api-transformers.test.ts tests/profile-view.test.ts`  
Expected: PASS

- [ ] **Step 5: 提交**

```bash
git add getoffer/src/views/Profile.vue getoffer/src/api/transformers.ts getoffer/tests/api-transformers.test.ts getoffer/tests/profile-view.test.ts
git commit -m "feat: show article moderation status in profile"
```

### Task 8: 全量验证与收尾

**Files:**
- Modify: `README.md`（如需补后台入口说明，可选）

- [ ] **Step 1: 跑后端回归测试**

Run: `./mvnw.cmd test`  
Expected: PASS

- [ ] **Step 2: 跑前端测试**

Run: `node --test tests/*.test.ts`  
Expected: PASS

- [ ] **Step 3: 跑类型检查和构建**

Run: `npm.cmd run type-check`  
Expected: PASS

Run: `npm.cmd run build`  
Expected: PASS

- [ ] **Step 4: 手工联调**

Run:

```bash
powershell -ExecutionPolicy Bypass -File scripts/start-backend.ps1
cd getoffer
npm.cmd run dev
```

检查：

- 普通用户登录后发文，前台首页看不到新文章
- 管理员访问 `/admin/login` 后能进入后台
- 后台文章页能看到待审核文章
- 点击“通过”后文章立即能在前台公开列表中出现
- 点击“拒绝”后个人中心显示“已拒绝”

- [ ] **Step 5: 最终提交**

```bash
git add .
git commit -m "feat: add admin mvp with article review flow"
```

---

## 自检

### Spec 覆盖检查

- 管理员独立入口：Task 5
- `role` 字段与管理员身份：Task 1、Task 5
- `status` 审核状态：Task 1、Task 2、Task 4、Task 7
- 前台仅展示 `APPROVED`：Task 2
- 后台数据概览：Task 3、Task 6
- 用户管理：Task 3、Task 6
- 文章审核：Task 4、Task 6
- AI 会话列表：Task 3、Task 6
- 个人中心状态展示：Task 7

### Placeholder 扫描

- 没有 `TODO` / `TBD`
- 每个任务都有明确文件、测试、命令和最小代码片段

### 类型一致性检查

- 角色常量统一为 `USER` / `ADMIN`
- 文章状态统一为 `PENDING` / `APPROVED` / `REJECTED`
- 管理后台统一挂在 `/api/v1/admin/**`
